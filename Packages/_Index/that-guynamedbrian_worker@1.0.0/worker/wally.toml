[package]
name = "that-guynamedbrian/worker"
description = "Simple utility module that provides worker coroutines that are cached after use"
license = "MIT"
authors = ["that-guynamedbrian/kobayashi"]
version = "1.0.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["src", "src/*",  "waiter.project.json", "wally.lock", "wally.toml"]